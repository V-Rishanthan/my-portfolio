import { createContext } from "react";

const FirestoreContext = createContext();

export const FirestoreProvider = ({ chilrend }) => {
  const manageHeroSection = async (profession, professionDes) => {};
};
